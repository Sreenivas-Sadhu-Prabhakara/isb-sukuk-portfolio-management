spring:
  application:
    name: isb-sukuk-portfolio-management

server:
  port: 8080
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
  endpoint:
    health:
      probes:
        enabled: true   # /actuator/health/liveness + /readiness for K8s probes
  info:
    env:
      enabled: true

info:
  isb:
    serviceDomain: "Sukuk Portfolio Management"
    businessArea: "Treasury Markets and Sukuk"
    businessDomain: "Sukuk"
    functionalPattern: "Manage"
    assetType: "Sukuk Portfolio"
    controlRecord: "Sukuk Portfolio Management Plan"

resilience4j:
  circuitbreaker:
    instances:
      serviceDomain:
        slidingWindowSize: 20
        failureRateThreshold: 50
        waitDurationInOpenState: 10s

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
